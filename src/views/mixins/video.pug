mixin video(info)
    a(href=`/videos/${info.id}`).video_mixin
        div.video_mixin_thumb
        div.video_mixin_data
            span.video_mixin_title=info.title
            div.video_mixin_meta
                span #{info.owner.name} ▪ 
                span #{info.meta.views} 회