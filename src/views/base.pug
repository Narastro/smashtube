doctype html 
html(lang="ko")
    head 
        title #{pageTitle} | Smashtube
        link(rel="stylesheet" href="https://unpkg.com/mvp.css")
    body
        header
            nav
                ul 
                    li
                        a(href="/videos/upload") Upload Video
                    li
                        a(href="/") Home
                    li
                        a(href="/search") Search
                    if loggedIn
                        li
                            a(href="/logout") Logout
                        li
                            a(href="/my-frofile") #{loggedInUser.name}의 Profile
                    else
                        li 
                            a(href="/join") Join
                        li
                            a(href="/login") Login
            h1=pageTitle 
        main
            block content
    include partials/footer