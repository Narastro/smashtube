extends base
include mixins/video

block content
    div.profile__data
        img(src=user.avatarUrl).avatar
        h3=user.name
    div.video_grid
        each video in user.videos
            +video(video)
        else
            span.empty_message 영상이 존재하지 않습니다.